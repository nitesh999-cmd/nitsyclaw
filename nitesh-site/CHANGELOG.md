# Changelog

All notable changes to this site are documented here. Format loosely follows
[Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

### Added

- Interactive Revenue Leak Scorecard (self-diagnostic lead magnet) with
  score-based routing to the matching offer.
- "With a system vs full-time hire vs do nothing" comparison (dark section).
- FAQ accordion (native `<details>`) with honest objection-handling answers.
- 3-field lead form that composes a pre-filled WhatsApp message (email fallback).
- Sticky mobile CTA bar (Call + WhatsApp), scroll-progress bar, scroll-reveal,
  industry marquee, animated gradient border on the signature offer.
- Branded Open Graph + Twitter share images with alt text.
- "Fix Framework" rendered as a connected timeline.
- Per-tier WhatsApp pre-fill on offer CTAs.
- Project hygiene: `.nvmrc`, `.editorconfig`, `.gitattributes`, Prettier config,
  `.env.example`, `vercel.json`, custom 404 + error boundary, PWA manifest,
  favicon/app icons.

### Changed

- Hero redesigned with aurora background, gradient headline, and a glass
  "lead-to-cash" product panel.
- Accessibility pass: AA-compliant WhatsApp buttons, visible focus rings,
  deepened result-gradient contrast, scoped `aria-live`.
- Honesty pass: removed the fabricated-looking salary figure, reworded the
  risk-reversal to an objective deliverable, softened results-implying copy.

### Notes / pending (need owner input — not fabricated)

- Audit pricing, real headshot, ABN, booking (calendar) link, and a real
  proof artifact are intentionally left for the owner to supply.
